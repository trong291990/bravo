<?php

namespace Admin;

use \Request;
use \Session;
use \Redirect;
use \Input;
use \View;
use \Auth;

class AuthController extends AdminBaseController {

    public function login() {
        if (Request::isMethod('GET')) {
            
        } else {
            
        }
    }

}

?>
